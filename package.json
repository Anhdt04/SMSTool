{
  "name": "smcs-lookup-tool",
  "version": "1.0.0",
  "description": "Tool tự động tra cứu số thuê bao trên hệ thống SMCS VNPT",
  "main": "server.js",
  "bin": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "test": "node test/test_logic.js",
    "build:sea": "node -e \"const fs=require('fs');fs.writeFileSync('src/public/embeddedAssets.js','module.exports='+JSON.stringify({html:fs.readFileSync('src/public/index.html','utf8'),css:fs.readFileSync('src/public/style.css','utf8'),js:fs.readFileSync('src/public/app.js','utf8')})+';')\" && npx esbuild server.js --bundle --platform=node --target=node20 --outfile=dist/bundle.js && node --experimental-sea-config sea-config.json && node -e \"require('fs').copyFileSync(process.execPath,'SMCS_Tool.exe')\" && npx postject SMCS_Tool.exe NODE_SEA_BLOB dist/sea-prep.blob --sentinel-fuse NODE_SEA_FUSE_fce680ab2cc467b6e072b8b5df1996b2",
    "build:exe": "npm run build:sea"
  },
  "pkg": {
    "assets": [
      "src/public/**/*",
      "src/**/*"
    ]
  },
  "dependencies": {
    "axios": "^1.7.9",
    "cheerio": "^1.0.0",
    "express": "^4.21.2"
  }
}
